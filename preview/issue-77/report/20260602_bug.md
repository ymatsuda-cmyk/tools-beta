# バグ作業状況 2026-06-02

実行日時: 2026-06-03 07:37:04

対象日: 2026-06-02  新規: 0件 / 更新: 6件

---

## 新規バグ

（該当なし）

---

## 更新バグ

### No.1 [36] 受信電文のレシート情報byte長が作成時と異なる値が出力され比較結果NGとなる
- **担当者**: 政次

**対応内容欄**
6/2 伊藤
結果コードも同様に対応が必要。
修正対象
■チェック仕様書
→完了
■シナリオ定義（通常）
■シナリオ定義（電源断）
■シナリオ定義（通信断）

### No.2 [48] シナリオ定義書の取引情報の再取得の受信電文期待値の結果コードが誤り
- **担当者**: 伊藤

**対応内容欄**
6/2 朝会
通信断のみ対応済
5/29
①CU列、CV列の取引情報の再取得時の結果コード、処理未了フラグの参照先を修正
----
・処理未了フラグ（CV列）
=IF(INDEX(シナリオ一覧,10,1)="電源断",LET(結果,IFERROR(INDEX(シナリオ一覧,MATCH($CG2117,INDEX(シナリオ一覧,,1),0),40),""),IF(結果="","",結果)),
IF(INDEX(シナリオ一覧,10,1)="通信断",LET(結果,IFERROR(INDEX(シナリオ一覧,MATCH($CG2117,INDEX(シナリオ一覧,,1),0),42),""),IF(結果="","",結果)),""))
・結果コード（CU列）
=IF(INDEX(シナリオ一覧,10,1)="電源断",LET(結果,IFERROR(INDEX(シナリオ一覧,MATCH($CG2117,INDEX(シナリオ一覧,,1),0),41),""),IF(結果="","",結果)),
IF(INDEX(シナリオ一覧,10,1)="通信断",LET(結果,IFERROR(INDEX(シナリオ一覧,MATCH($CG2117,INDEX(シナリオ一覧,,1),0),43),""),IF(結果="","",結果)),""))
----
②名前の定義がAO列までの範囲となっているので、AT列まで範囲を広げる。

### No.3 [49] メインの受信電文がシナリオ定義書に入力されない
- **担当者**: 政次

**原因欄**
6/2 政次
自動化項番5,7,8,9で発生
いずれも操作2で受信電文がかえってきていない。
証跡を格納済

### No.4 [51] 期待値（店舗名、電話番号）は数式が入る項目だが色の設定が無い
- **担当者**: 政次

**対応内容欄**
6/2 通信断も対応

### No.5 [52] 期待値（処理未了フラグ）を値貼り付けしても赤色セルにならない
- **担当者**: 政次

**対応内容欄**
6/2 高橋 通信断も対応

### No.6 [53] logcatログが1件も生成されない時がある
- **担当者**: 高橋

**原因欄**
6/2 高橋
バグ票運用以前、課題#43で対応した筈の「adb コマンドには -d を付ける」対応が、網羅されておらず、logcatログの出力コマンドが 「adb logcat」のままだった。対象スクリプト：logcat.ps1。
よって悉皆調査として、すべてのスクリプト(.ps1および .bat)およびRPAのソース（メインおよびサブフロー)に対し、"adb" および "scrcpy" の検索を行い、修正対象箇所を洗い出し。
調査結果は別紙フォルダ配下のExcel参照
