・カスタマイズタブではなく、＋アイコンでタブを追加できるようにする。追加したタブは設定から削除できる。
・カードのタブ移動、非表示／再表示の切り替えは設定画面から行えるようにする。デフォルトタブ（リンク、稼働状況、情報）は指定できないようにする。選択できるのは追加したタブと解除のみ。
・タブを削除した際に、表示タブがない場合は、デフォルト（リンク、稼働状況、情報）に表示する。
・apiへのアクセスができるよう確認すること（現在、Failed to fetchエラーが出てる）