# 営業報告 Officeアドイン

Excelの「営業報告」シートを、タスクペインから **一覧／カンバン／集計** で操作できるアドインです。
新規入力はヘッダー右上の「＋新規入力」ボタンから行います（再読み込みボタンの左）。

**設定フラグ**: `app.js` 冒頭の `ENABLE_KANBAN_DND` を `true` にするとカンバンのドラッグ＆ドロップが有効になります（デフォルト `false`）。

## 画面構成

| 画面 | 機能 |
|---|---|
| 一覧 | 種別別グループ表示（**優先度列は左端**）。ワークフロー完了後の状態は「状態（m/d）」表示。状態フィルタは**複数選択可**。行クリック／右クリックで編集画面 |
| カンバン | 種別ごとのワークフローに沿った状態レーン（種別・状態フィルタは非表示）。DnDは `ENABLE_KANBAN_DND` フラグで制御（既定OFF） |
| 集計 | 保守状況／見積状況／受注状況／稼働状況 |
| ＋新規入力 | 起票情報のみ入力（対応状況は起票後に編集画面で入力） |

## 編集画面（ステージタブ式）

チケットの状態に応じたタブが活性化し、それ以外は 🔒 非活性になります。
**備考はタブの外にあり、どのステージでも編集できます。**

| タブ | 内容 | 完了時の遷移 |
|---|---|---|
| 起票 | 発生日・**起票者**・窓口・問合せ内容（新規入力時は担当者ではなく起票者を入力。担当者の初期値=起票者） | － |
| 対応中（保守・瑕疵・調整） | **対応担当者**（変更で担当者欄も更新）＋区分（保守対応のみ必須）＋対応状況 | 対応完了チェック→完了日を記録しチケット完了（開始日の右に完了日を表示） |
| 見積中（見積り） | **見積担当者**＋対応状況＋工数・価格（税抜）・**税込自動計算**・根拠 | 見積完了チェック→**見積完了日**を記録し確認中へ |
| 検討中（プリセールス） | **検討担当者**＋対応状況（検討・検証・提案作成など） | 検討完了チェック→**検討完了日**を記録し商談中へ |
| 商談中 | **窓口**＋商談状況 | 商談完了チェック→**商談完了日**を記録し確認中へ |
| 確認中 | 確認状況＋結果チェック | 「受注確定を完了にする」→**確認完了日**を記録・ステータスは確認中のまま受注タブ活性化／「失注」→確認完了日＋完了日を記録しチケット完了 |
| 受注 | 納品日・**計上日**・最終工数・最終価格（税込自動計算）・受注条件 | 完了チェック＋登録→ステータス「受注」・チケット完了 |

- 状態「新規」では対応状況欄は空のまま。**対応状況を記入して登録すると** 対応中／見積中／作成中へ遷移し、開始日（T列）を記録します。
- 完了したステージのタブは非活性となり、入力内容は参照のみ可能です。

## ワークフロー

- 保守対応・瑕疵対応・調整：`新規 → 対応中 → 完了`
- 見積り：`新規 → 見積中 → 確認中 → 受注 or 失注`
- プリセールス：`新規 → 検討中 → 商談中 → 確認中 → 受注 or 失注`
- どの状態からも「保留」に退避可能（カンバンDnD）

## 集計

- 集計期間は**期単位（10月〜翌9月、第37期=2025/10〜2026/09）**。◀▶ボタンで期を切り替えられます。
- **保守状況**: 保守対応＋瑕疵対応の月次推移を「発生」「完了」2系列のグラフで可視化。明細表に保守／瑕疵の内訳。
- **見積状況**: 見積り＋プリセールスを 新規→検討中→見積中→商談中→確認中→失注→受注 の順に件数・金額集計（受注は最終価格）。
- **受注状況**（旧: 売上予測）: 受注確定案件を**計上日ベース**で月別集計・グラフ化。計上日未入力の受注は警告表示。
- **稼働状況**: 期内の稼働キャパシティを可視化。サブタブは「全体キャパ／対応工数（保守）／受託工数」の3つ。
  - **全体キャパ**: 月次の基本稼働（営業日×体制シートの稼働人数）に対し、対応工数（保守）＋受託工数の合計がどれだけ溢れたかを積み上げ棒＋線グラフと月別内訳表で表示。
  - **対応工数（保守）**: 保守対応／瑕疵対応／調整の対応工数を、取引先ごとの**保守費（月額）・許容工数（人日/月）**と比較。表示は「月次／累計」を切替可能。分類チップ（すべて／保守／瑕疵／調整）で絞込み。見積り・プリセールスは受注確定前の工数を記録する列が現状の帳票に無いため対象外（受注後の工数は受託工数タブで扱う）。
  - **受託工数**: 取引先ごとの合計受注額・見積工数・実績（着手日〜納品日/完了日の日数比按分）を表示。行クリックで案件ごとの予実明細を下部に展開。
  - 基本稼働の算出には**体制シート**（稼働人数）と**祝日シート**（土日祝を除いた営業日数）を使用します。体制シートが未整備の場合は警告表示。
  - 「その他（社内業務・営業活動等）」の工数は現状未実装のため、実際の基本稼働との差はここに表示される値より小さくなる場合があります。

## シート仕様

### 営業報告シート（1行=1案件、1行目=ヘッダー）

カラム名はアプリ起動時に自動で正規化されます（見出しが空欄／旧表記の場合は正規の名称に書き直されます。列の並び順・位置は変わりません）。

| 区分 | 列 | 見出し | 用途 |
|---|---|---|---|
| 基本情報 | A | ID | 案件番号（顧客コード-通番、自動採番） |
| | B | 取引先 | |
| | C | No（未使用） | 旧様式の互換用。現在は空欄のまま |
| | D | 種別 | 保守対応／瑕疵対応／見積り／プリセールス／調整 |
| | E | 状態 | ワークフロー状態 |
| | F | 発生日 | |
| | G | 完了日 | チケット完了時に自動セット |
| | H | 担当者 | 現在の対応者。各ステージ担当者欄の変更で自動更新 |
| | I | 窓口 | |
| | J | 優先度 | 高／中／低 |
| 見積・受注 | K | 見積工数（人日） | |
| | L | 見積金額（税抜） | |
| | M | 受注区分 | 受注／失注 |
| | N | 納品日 | |
| 内容・メモ | O | 問合せ・提案内容 | |
| | P | 進捗状況 | 対応中／見積中／検討中で共通利用する経過メモ |
| | Q | 備考 | 編集画面でどのステージでも編集可能 |
| | R | （未使用） | 予備列 |
| ステージ詳細 | S | 区分（問合せ／改修） | 保守対応のみ使用 |
| | T | 着手日 | 新規→作業ステージに遷移した日 |
| | U | 見積根拠 | |
| | V | 商談状況 | |
| | W | 確認状況 | |
| | X | 計上日 | 受注状況（売上）集計の集計キー |
| | Y | 最終工数（人日） | |
| | Z | 最終価格（税抜） | |
| | AA | 受注条件 | |
| 管理・完了日 | AB | 起票者 | 新規入力時の入力者。担当者の初期値になる |
| | AC | 見積完了日 | |
| | AD | 検討完了日 | |
| | AE | 商談完了日 | |
| | AF | 確認完了日 | |
| | AG | 受注確定日 | |
| | AH | 受託開始日 | |
| | AI | 完了予定日 | |
| 更新管理 | AJ | 最終更新日 | 保存（新規登録・ステージ編集・状態変更）の都度、自動で今日の日付を記録 |

### 顧客マスタシート（無ければ自動作成）

A:顧客コード（英字2〜4文字） B:取引先名 C:窓口 D:備考 E:保守費（月額） F:許容工数（人日/月）
案件番号は「顧客コード-通番」（例: KM-01）で自動採番します。
E・Fは稼働状況「対応工数（保守）」の保守費比較に使用します（未入力の取引先は比較対象外）。
旧版（4列）のシートは起動時にヘッダーを自動でE・F列付きに書き直します（既存データは保持）。

### 体制シート（無ければ自動作成）

A:氏名 B:稼働開始日 C:稼働終了日
稼働状況の「基本稼働（人日）」計算に使う、月ごとの稼働人数の元データです。稼働終了日が空欄の人員は現在も稼働中として扱います。開始日・終了日と対象月の重なりで、月ごとの稼働人数をカウントします。

### 祝日シート（無ければ自動作成）

A:日付 B:名称
稼働状況の営業日数（8h/日ベースの基本稼働）算出時に、土日に加えてこのシートに登録された日付を非稼働日として除外します。

## デプロイ手順（GitHub Pages）

1. このフォルダ一式を `tools/addin/eigyo/` として GitHub Pages リポジトリに配置
2. `manifest.xml` をサイドロード、または Microsoft 365 管理センターから組織展開
3. Excelで対象ブックを開き、ホームタブの「営業報告」ボタンでタスクペインを表示

## デモモード

`index.html` をブラウザで直接開くと、Excelに接続せずサンプルデータで全機能を試せます（DEMOバッジ表示）。


## サイドメニュー（共通スライドメニュー）

ヘッダー左のハンバーカーボタン（≡）から、他アドインと共通のスライドメニューを開きます。
`tools/addin/common/slide-menu.js` と `tools/addin/common/menu.json` を遅延ロードして表示します。

- メニューには「操作（再読み込み）」と、共通の「ツール（カンバン → 営業報告 → バグ管理）」「リンク（使い方）」が並びます。
- カンバンの後ろに **営業報告**（id: `eigyo`）を追加した `menu.json` を同梱しています。共通ディレクトリ `tools/addin/common/menu.json` をこの内容に差し替えてください（全アドインのメニューに一斉反映されます）。
- 現在表示中のアドイン（営業報告）はメニュー内で強調表示されます（`currentId: "eigyo"`）。

## この版の主な変更（rev_20260710_f）

### 詳細画面
- **削除ボタン**を追加。押下時に確認メッセージを表示し、OKで状態を「削除」に変更（一覧・カンバンから非表示になる）。
- 入力内容を変更した場合、画面外クリックでは閉じない（誤操作防止）。✕ボタンは確認ダイアログを表示。
- 一度登録した案件は**種別を変更不可**（グレー表示）。
- 登録するとタスクペインを閉じる。Excelの使用範囲が下方向に広がらないよう、読み書き範囲を使用範囲ベースに変更。

### 一覧・カンバン
- 状態が「削除」の案件は表示しない（状態フィルタの選択肢からも除外）。
- **左クリック**で該当のExcel行に移動し、その行を選択状態にする。
- 一覧はクリックした行をハイライト（排他・単一選択）。
- 詳細画面を開くには**右クリック**または**ダブルクリック**。
- 確認中で受注を選んだ案件も、状態表示は「確認中」に統一（受注バッジを撤去）。

### 保守の対応中
- 対応状況の下に「対応工数（人日）」欄を追加。

## ファイル構成

```
eigyo/
├── manifest.xml     … アドインマニフェスト
├── index.html       … タスクペイン本体
├── app.js           … ロジック（Excel I/O・ステージタブ・カンバン・集計）
├── style.css        … スタイル
├── commands.html/js … リボンコマンド用
└── icons/           … アイコン
```

## この版の主な変更（rev_20260710_g）
- Office環境で `window.confirm`/`alert` が使えない問題に対応し、アプリ内ダイアログに置き換え（削除確認・変更破棄確認・各種通知）。
- 集計「受注状況」「見積状況」「保守状況」から**状態「削除」の案件を除外**（売上等に計上されない）。
- 保守状況に**対応工数（人日）の月次集計**を追加し、工数推移を折れ線グラフで可視化。KPIに対応工数計、月別明細に対応工数列を追加。

## この版の主な変更（rev_20260710_h）
- **受注確定金額**を、受注区分ではなく**状態欄が「受注」**の案件で計上するよう変更。
- **見積状況**：状態別集計の件数をクリックすると、その状態の**見積一覧**が下に展開。一覧の行をクリックすると該当案件の詳細画面を開く。
- **受注状況**：受注案件一覧の行をクリックすると該当案件の詳細画面を開く。
- **工数表示チェックボックス**を集計セグメント行の左端に追加。保守状況の「対応工数計KPI・工数推移グラフ・明細の対応工数列」をまとめて表示ON/OFF切替。

## この版の主な変更（rev_20260710_i）— 受託中ステージとWBS連携（案A）
- **ワークフロー拡張**: 見積り／プリセールスは `受注 → 受託中 → 完了` に。受注タブの最終登録でチケット完了せず「受注」状態となり、受託中タブが活性化。
- **受託中タブ**（2段階）:
  1. 状態=受注: 開始日（必須）・完了予定日を入力し「開始日を確定し、受託中にする」で状態=受託中へ。
  2. 状態=受託中: 「対応完了」チェックで完了日を記録しチケット完了。
  - **計上日・納品日**はどちらの段階でも変更可能。
  - **WBS状況**: wbsシートの小分類(B列)=案件番号のタスクを 未着手／対応中／完了／保留 の件数チップで表示（判定はカンバンと同一: S列実績終了=完了、O列備考▲=保留、R列実績開始=対応中）。
- **新列**: AG=受注確定日、AH=受託開始日（起動時にヘッダー自動整備）。
- 集計: 受注確定金額・受注状況は 状態が受注／受託中／完了 の案件を計上。見積状況の並びに受託中・完了を追加。
- タスクの実体管理はカンバンアドインの「タスク追加」（大分類=受注、小分類=案件番号）で行います。

## この版の主な変更（rev_20260710_j）— 受託スケジュール（ガントチャート）
- メインタブに**スケジュール**を追加（カンバンと集計の間）。中は「受託スケジュール（ガント）／受注状況グラフ」のサブタブ切替。
- **対象**: 状態が受注／受託中／完了の案件。バー=受託開始日(AH)〜納品日(N)（完了は完了日まで）。開始日・納品日が未確定の案件は斜線の仮表示。
- **操作**: バー両端ドラッグ=開始/終了の変更、本体ドラッグ=期間移動（AH列・N列へ即時書き戻し。完了案件は編集不可）。背景ドラッグで期間パン。細いバーは誤操作防止のため移動のみ（ズームインで両端編集可）。
- **表示**: 期(10月〜翌9月)単位で◀▶切替、ズーム12/6/3/1ヶ月（今日を中心に自動スクロール）、本日線＋今日チップ、開始/終了のm/d表示、タイトルはバー内に省略表示、納品日を▲m/dでプロット、凡例付き。
- カレンダーヘッダーは縦スクロールで上部固定（sticky）、案件ラベル列は横パンで左固定。
- 案件ラベルをクリックすると詳細画面を開きます。

## この版の主な変更（rev_20260710_k）

### 状態遷移
- 確認中タブ「受注確定を完了にする」→ 登録で状態が **受注** に。
- 受注タブ「この内容で登録し、受注確定する」→ 登録で状態が **受託中** に。

### 受託中タブ
- 開始日・完了予定日・納品日・計上日をすべて編集可能に（**完了予定日**は新列 AI）。
- 相関チェック: **開始日 ≦ 完了予定日 ≦ 納品日 ≦ 計上日**。違反時は該当項目を明示。
- 備考のタスク（□未着手/◎対応中/■完了）を **3レーンのカンバン** で表示。ドラッグでレーン移動でき、備考欄と双方向同期。

### フィルタ
- 種別・取引先を状態と同じ **複数選択** に統一。
- フィルタ条件を **Cookie に保存・復元**（次回起動時も維持）。

### スケジュール（ガント）
- 右クリックで行選択（Excel該当行へジャンプ）、左クリックで詳細画面。
- カレンダーに **日付** を追加。ズーム別に自動調整（12ヶ月=なし／6ヶ月=隔週1・15日／3ヶ月=毎週／1ヶ月=毎週＋曜日）。週頭は月曜起点。
- 受注状況グラフのサブタブを撤去（受託スケジュールのみ）。
- 表示期間（ズーム・期）を **Cookie に保存**。

### 集計
- 保守状況: 発生・完了（棒）と対応工数（折れ線・右軸）を **1つの複合グラフ** に統合。工数表示チェックで折れ線ON/OFF。
- 見積一覧・受注状況の一覧: 右クリックで行選択、左クリックで詳細画面。

### その他
- 登録時に対象行の折り返しを無効化・行高さを固定し、Excelの行が縦に広がらないように。

## 補足（rev_20260710_l）
- 受託中タブのWBS表示は「WBS状況（未着手／対応中／完了／保留の件数チップ）」です。個々のタスク管理およびサブタスクカンバンは**カンバンアドイン**側（wbsシートのタスク備考モーダル内）で行います。

## スケジュール仕様の修正（rev_20260710_m）
- 案件ラベルの操作を修正：**左クリック＝行選択（Excel該当行へジャンプ）／右クリック＝詳細画面**。
- バーは **開始日(AH列＝受託開始日) 〜 完了予定日(AI列)** を表します。
  - 両端ドラッグ＝開始日／完了予定日の個別変更。
  - 本体ドラッグ＝開始日と完了予定日が連動して移動。
  - いずれも **AH列・AI列へ即時書き戻し**。**納品日(N列)は連動しません**（▲マーカーとして別途表示）。
- 表示期間（12/6/3/1ヶ月ボタン・期）は **Cookie に保存**（GitHub Pages https 上で永続化）。

## 一覧の種別グループ開閉・前週実績グレーアウト（rev_20260720_d）
- 一覧の「種別」フィルタ（ドロップダウン）を廃止。代わりに一覧はもともと種別ごとにグループ表示していたため、**グループ見出しをクリックすると開閉**できるように変更（開閉状態はブラウザに保存され次回起動時も復元）。
- 一覧・カンバン共通で「先週実績」トグルボタンを追加。押すと**先週（月〜日）に更新されなかった案件を非表示にはせず、薄いグレー表示（opacity 0.4）**にする。もう一度押すと解除。
- 新規列 **AJ:最終更新日** を追加。案件の新規登録・ステージ編集・状態変更（保存操作）の都度、自動で当日の日付を記録する。「先週実績」判定はこの列を参照する。

## 稼働状況タブの追加（rev_20260820_c1e9d02）
- 集計に**稼働状況**タブを新設（受注状況の右）。サブタブ「全体キャパ／対応工数（保守）／受託工数」。期切替（◀▶）は他タブと同様。
- **全体キャパ**: 月次の基本稼働（営業日×要員数）に対する対応工数（保守）＋受託工数の積み上げ棒＋基本稼働の線グラフ、月別内訳表（営業日・要員数・基本稼働・対応工数・受託工数・合計・差分）。経過月中に溢れた月数・溢れ合計をKPI表示。
- **対応工数（保守）**: 保守対応／瑕疵対応／調整の対応工数を取引先ごとに集計し、顧客マスタの保守費（月額）・許容工数（人日/月）と比較。表示切替は**「月次」「累計」**（旧案A/案Cの案から名称変更）。分類チップ（すべて／保守／瑕疵／調整）で絞込み可能。取引先ごとに月次バー（許容超過は赤）または累計バーと、超過回数・保守費乖離額を表示。
- **受託工数**: 見積り／プリセールスの受注後案件（受託中・完了）を対象に、取引先ごとの合計受注額・見積工数（Y列:最終工数）・実績工数（AN列:受託工数、着手日〜完了予定日/完了日の日数比按分）を集計。行クリックで案件別の予実明細（見積工数・実績工数・消化率・見積残・金額・状態）を下部に展開。
- **新規シート**: **体制**（氏名・稼働開始日・稼働終了日）で月次の稼働要員数を管理。**祝日**（日付・名称）で土日以外の非稼働日を管理し、基本稼働（8h/日換算の営業日数×要員数）の算出に使用。どちらも未作成の場合はヘッダー付きで自動作成し、体制シートが空の場合は稼働状況タブに警告を表示。
- **顧客マスタシートに列を追加**: E:保守費（月額） F:許容工数（人日/月）。既存4列シートは起動時に自動でヘッダーを拡張（データは保持）。
- 「その他（社内業務・営業活動等）」の工数管理は仕組みが未整理のため今回は対象外。実際の基本稼働との差は、その分だけ本タブの表示値より小さくなる場合があります。

## 稼働状況タブの改修（rev_20260821_d4a2f19）
- **対応工数（保守）の分類フィルタを拡張**: 「見積り」「プリセールス」を瑕疵と調整の間に追加（すべて／保守／瑕疵／見積り／プリセールス／調整）。見積り・プリセールスも他の種別と同じ対応工数（AM列）を集計対象とし、対象は受注確定前の段階（新規・見積中・検討中・確認中など）まで（受注・受託中・完了になった案件は「受託工数」タブ側でカウントするため、二重計上を避けるためここでは対象外）。
- **分類フィルタに複数選択トグルを追加**: OFF（既定）は従来どおり単一選択、ONにすると保守・瑕疵など複数の分類を組み合わせて絞り込めます。
- **保守費・許容工数が未設定の取引先も表示**: 従来は一覧から除外し件数のみ注記していたが、「保守費 未設定の取引先」欄を新設し実績（人日）のみを表示するようにした（乖離は算出しない）。
- **保守費乖離を独立列に**: 各取引先行の右端に「保守費乖離」列を配置（従来はバッジ下に縦積み表示）。
- **KPIの並び順を変更**: 「保守費との乖離／実績・許容／超過している取引先／保守費設定済み取引先」の順に変更。
- **受託工数に状態フィルタを追加**: すべて／受託中／完了で絞り込み可能に（従来は常に受託中・完了を合算）。
- **受託工数のKPIラベルを変更**: 「見積 / 実績 工数（人日）」→「実績 / 見積 工数（人日）」（値の並びも実績を先に統一）。
- **全体キャパ／対応工数（保守）／受託工数の切替をタブ形式に変更**: ボタンからタブ（下線スタイル）に変更し、期セレクタとあわせて画面上部に固定表示（スクロールしても常に見える）。
- 全体キャパの「対応工数（保守）」集計対象は従来どおり保守対応・瑕疵対応・調整の3種のみに固定（分類フィルタの拡張とは独立）。

## 対応工数（保守）／受託工数の集計元カラムを修正（rev_20260821_e7c0b3d）
- **対応工数（保守）に見積り／プリセールスを含める場合の集計元をAM列（対応工数）に統一**: 従来案のK列（見積工数）ベースから変更。見積り・プリセールスの各ステージで入力する対応工数（人日）をそのまま集計します。
- **受託工数タブの「見積」をY列（最終工数）ベースに修正**: 従来AM列（対応工数）を流用していたが、受注確定時に入力するY列（最終工数）が正しい見積基準のため変更。取引先集計・見積超過判定・KPI・案件別明細（消化率・見積残）・全体キャパの受託工数按分（受託中ステータス分）すべてに適用。

## 対応工数（保守）の状態フィルタを撤廃（rev_20260821_f2b9a41）
- 対応工数（保守）に見積り／プリセールスを含める場合、当初は「受注・受託中・完了になった案件は対象外（受託工数タブと二重計上しないため）」としていたが、**状態を問わずAM列（対応工数）を計上する**よう変更（保守対応/瑕疵対応/調整と同じ扱いに統一）。
- そのため、受注確定後の案件は「対応工数（保守）」（AM列）と「受託工数」（Y列・AN列）の両方に数字が出る場合がある。列としては別の値だが、同じ案件の工数が両タブに現れる点は把握のうえ利用すること。

## 集計タブのサブタブ・稼働状況ヘッダーを固定表示に（rev_20260821_a8c7e52）
- 集計画面のサブタブ（保守状況／見積状況／受注状況／稼働状況）を `position: sticky` で固定し、下のコンテンツをスクロールしてもサブタブが常に見える状態に変更。
- 稼働状況タブでは、サブタブの直下に期セレクタ＋ビュータブ（全体キャパ／対応工数（保守）／受託工数）が重ならず連続して固定表示されるよう、両者の固定オフセットを調整（サブタブの高さ＋余白ぶんを稼働状況側のオフセットに反映）。
- 一覧／カンバン／スケジュール／集計の画面切替タブは元々スクロール領域の外にあり、対象外（従来から固定表示）。
- Playwrightでの実スクロール確認済み：稼働状況タブ（全体キャパ／対応工数（保守）／受託工数の3ビューすべて）に加え、保守状況／見積状況／受注状況の他3サブタブでも `.agg-seg` が正しく固定され、画面崩れ・エラーがないことを確認。

## 固定表示の隙間を背景色で埋める修正（rev_20260821_b3d6f19）
- `.agg-seg`（集計サブタブ）の `margin-bottom: 12px` の部分は背景が塗られないため、固定表示中にスクロールすると、その12px分だけ下のコンテンツが透けて見えてしまっていた（DevToolsのマージン表示＝オレンジ部分に相当）。
- `.agg-seg::after` で margin 分をページ背景と同じ `#f7f8f9` で塗りつぶし、隙間から下のコンテンツが透けないよう修正。
- Playwrightでスクロール後の実ピクセル色を確認し、隙間部分が `#f7f8f9`（透明ではない）になっていることを検証済み。

## div.agg-seg上部の隙間修正・対応工数（保守）／受託工数に取引先フィルタを追加（rev_20260821_c9e14a7）
- `.agg-seg` は `.pane` の `padding-top: 10px` の分だけ、固定表示時に上端との間に隙間ができ、その隙間から下のコンテンツがスクロールで透けて見えていた（`top: 0` のままでは padding 分が固定表示の対象に含まれないため）。`top: -10px` に変更し、`.pane` の最上部（padding含む）まで完全に詰めて固定されるよう修正。合わせて `.kado-sticky` の `top` も 45px→35px に調整し、両者の間隔（12px）は変更前と同じまま。
- 対応工数（保守）タブに「取引先」フィルタ（ドロップダウン、単一選択・すべて可）を追加。選択すると、保守費設定済み・未設定の両方の表を該当取引先のみに絞り込む（既存の分類フィルタと併用可）。
- 受託工数タブにも同様に「取引先」フィルタを追加。状態フィルタ（すべて／受託中／完了）と併用可能。
- Playwrightで全4サブタブ・稼働状況3ビューの回帰確認、取引先フィルタの選択結果（該当取引先のみに絞り込まれること）を確認済み。

## 集計サブタブの上部の隙間をスクロール前後で一定にする修正（rev_20260821_d5f8b30）
- 前回の修正（`top: -10px`）は、スクロールして固定表示された状態でのみ `.pane` の padding-top(10px) 分の隙間が消える仕様になっており、スクロール前（隙間あり）とスクロール後（隙間なし）で見た目が変わってしまっていた。
- `.agg-seg` に `margin-top: -10px; padding-top: 10px;` を設定し、`.pane` の padding-top による隙間を打ち消した上で、同じ幅の隙間を `.agg-seg` 自身の（背景色付きの）ボックス内に取り込むよう変更。これにより、スクロール前後で隙間の見え方が変化しなくなった（常に同じ10pxの隙間で、常に背景色で塗られている）。
- `.agg-seg` の高さが33px→43pxに変わったため、`.kado-sticky` の `top` を 35px→55px に再調整（直下に重ならず固定される間隔は変更前と同じ12pxを維持）。
- Playwrightでスクロール位置0/10/50/300pxそれぞれの座標を検証し、隙間の大きさが常に同じ（上10px・中12px）であることを確認済み。他3サブタブでも同様に固定位置が変化しないことを確認済み。

## 対応工数（保守）／受託工数の取引先フィルタをチップ形式に変更（rev_20260821_e2a6c94）
- 取引先フィルタをドロップダウン（`<select>`）から、分類・状態フィルタと同じ「すべて」＋取引先名チップのボタン形式に変更（単一選択、選択中は緑色でハイライト）。
- 取引先の候補が多い場合はチップが折り返して複数行になるため、ラベル「取引先」を上段・チップ群を下段に配置するレイアウト（`.af-row-col`）に変更。
- 動作はドロップダウン版と同じ（対応工数（保守）・受託工数それぞれで単一取引先に絞り込み）。Playwrightでチップ選択→絞り込み結果を確認済み。

## メインタブ〜集計サブタブ間の隙間をグレーに変更（rev_20260821_f6b1d80）
- `.agg-seg`（集計サブタブ）の背景色を、ページ背景色 `#f7f8f9`（ほぼ白）から、パレットで定義済みのグレー `#eef1f3` に変更。ヘッダー・メインタブ（一覧／カンバン／スケジュール／集計）とサブタブ（保守状況／見積状況／受注状況／稼働状況）の間の隙間が、白ではなくはっきりグレーで塗られるようになった。
- 未選択のサブタブ自体の背景色（`#eef2f4`）とほぼ同色のため、隙間とタブ行が一体のグレーの帯として見える。

## 集計画面の固定表示を position:sticky から実レイアウト分離に変更（rev_20260821_a1c9f37）
- これまで `position: sticky` で実装していた「サブタブ（保守状況／見積状況／受注状況／稼働状況）」「稼働状況の期セレクタ＋ビュータブ」の固定表示を、レイアウトを分離する方式に変更した。
  - `#pane-agg` を「固定ヘッダー（`.agg-seg` ＋ `#agg-subfixed`）／スクロール領域（`.agg-scroll`）」の2段構成（flexbox）にし、`.agg-scroll` にだけ `overflow: auto` を設定。
  - 稼働状況の期セレクタ＋ビュータブ（`.kado-fixed`、旧`.kado-sticky`）は `#agg-subfixed` に描画され、他の3サブタブでは空になる。
  - これにより、スクロールバーが `.agg-scroll` の範囲（サブタブ、および稼働状況では期セレクタ＋ビュータブより下）だけに表示されるようになった。
  - sticky特有のオフセット計算・隙間の透け対策（`margin-top:-10px`／`padding-top`／`::after`など）が不要になり、CSSが簡潔になった。
  - 隙間の背景色は要望により `#f7f8f9`（ページ背景色）に戻した。
- Playwrightで全4サブタブ・稼働状況3ビューの回帰確認、取引先フィルタ等の動作確認、`.agg-seg`がスクロールで一切動かないことを確認済み。

## 対応工数（保守）「分類」フィルタ行の見た目を受託工数「状態」行と統一（rev_20260821_b7e42d1）
- 対応工数（保守）の「分類」フィルタ行は、ラベル＋チップを内側のdivで包んだ上で複数選択トグルを`justify-content:space-between`で右寄せする構造になっており、受託工数の「状態」行（`.af-row`直下にラベル＋チップのみ）とDOM構造が異なっていた。
- 内側のdivを廃止し、`.af-row`の直下にラベル・チップ・複数選択トグル（`margin-left:auto`で右寄せ）を並べる、状態行と同じ構造に統一。狭い幅で折り返した際の見た目（ラベル→チップ→トグルの順に積む）も揃った。
- 表示上の変化はなく、Playwrightで複数選択トグル・分類チップ選択の動作に問題がないことを確認済み。

## 取引先フィルタのチップの左端位置を他行と揃える（rev_20260821_c3d09e6）
- 「取引先」フィルタ行に使っていた `.af-row-col`（ラベルを上段・チップを下段に積む）は、チップの左端がラベル自身の位置（左端）に揃ってしまい、「分類」「状態」「表示」など他のフィルタ行（ラベル分の幅の右からチップが始まる）とチップの開始位置がずれて見えていた。
- `.af-row-col` を外し、他の行と同じ通常の `.af-row`（ラベル＋チップを横並びにし、幅が足りない時だけ自然に折り返す）に変更。折り返した場合もチップは他行と同じ位置から始まるようになり、取引先の候補が多くても左端が揃う。
- Playwrightで広い幅・狭い幅（タスクペイン相当）の両方を確認し、全フィルタ行のチップの左端が揃っていることを確認済み。

## 「取引先別 対応工数」をグリッド整列デザインに変更（rev_20260821_d8f21b5）
- デザイン改善案（3案：ヒーロー数値強調型／グリッド整列型／進捗バー統合型）を提示し、グリッド整列型（案B）を採用。
- 取引先別 対応工数の各行を、列見出し（取引先／推移／消化状況／保守費乖離）付きのグリッドレイアウトに変更。取引先が複数並んだ際に、グラフ・消化状況・保守費乖離の数値が縦にきれいに揃うようになった（保守費・許容工数未設定の取引先の行も同じグリッドに揃う）。
- Excelタスクペイン相当の狭い幅（700px以下）では、グリッドのまま横スクロールさせると主要な数値（保守費乖離など）が隠れてしまうため、名前＋乖離額を1行目・グラフ／消化状況を縦積みにするレイアウトに自動的に切り替わるようにした（`@media (max-width: 700px)`）。
- Playwrightで広い幅・狭い幅（420px）双方の見た目、月次／累計表示切替、保守費未設定の取引先セクションを確認済み。全4サブタブ・稼働状況3ビューの回帰確認も実施。

## 対応工数（保守）に「保守契約外（人日）」KPIを追加（rev_20260821_e4a90c2）
- 保守費・許容工数が未設定の取引先（＝「保守費 未設定の取引先」欄に表示される取引先）の実績工数（経過月分）を合計し、KPIカード「保守契約外（人日・経過Nヶ月）」として追加（取引先フィルタ選択時はその1社分のみ）。
- このKPIは値の大小に関わらず常に警告色（`kpi-alert`：枠上部・数値ともに赤）で表示し、他の緑色KPIと視覚的に区別。保守契約の枠外で発生している工数であることを常に目立たせる意図。
- 表示位置はKPI行の最後（保守費乖離／実績・許容／超過取引先数／保守費設定済み取引先数の後）。フッター注記にもKPIの算出根拠を追記。
- Playwrightで値（保守費未設定取引先の実績合計と一致）・警告色スタイルの適用・他3KPIの色分けが従来通りであることを確認済み。全4サブタブ・稼働状況3ビューの回帰確認も実施。

## 対応工数（保守）KPIラベル変更／全体キャパの対応工数(保守)・受託工数の算出仕様変更（rev_20260821_f83d16e）
- 対応工数（保守）のKPIラベルを変更：「保守費との乖離（円）」→「残予算（円）」、「実績 / 許容（人日・経過Nヶ月）」→「実績 / 総工数（人日・経過Nヶ月）」。算出内容・色分けロジックは変更なし。
- 全体キャパの「対応工数（保守）」系列（`kadoHoshuMonthly`）を、保守対応・瑕疵対応・調整の3分類のみから、見積り・プリセールスを含む5分類（`KADO_HOSHU_TYPES`）に変更。見積り・プリセールスの対応工数（AM列）も、対応工数（保守）タブと同様に状態を問わず全体キャパの積み上げに含まれるようになった。
- 全体キャパの「受託工数」系列（`kadoUketakuMonthly`）を変更。従来は受託中は見積工数（Y列＝最終工数）・完了は実績工数（AN列）を、受託開始日〜完了(予定)日の日数比で月次按分していたが、受託中・完了とも実績工数（AN列）を用い、受託開始日の月にまとめて計上する方式に変更。
- 上記2点の変更により、見積り/プリセールス案件が受託中・完了まで進んでいる場合、対応工数（保守）と受託工数の両方に工数が計上され二重計上になり得るため、フッター注記にその旨を明記した。
- 受託工数タブ（`renderKadoUketaku`）側の見積／実績算出ロジックは今回変更していない（全体キャパの積み上げ用系列のみの変更）。
- Playwrightで全体キャパの実績合計値が変更前後で変化すること（見積り・プリセールス分・按分方式変更分が反映されること）、対応工数（保守）タブ・受託工数タブの数値が変化していないこと、全4サブタブ・稼働状況3ビューでpageerrorが出ないことを確認済み。

## KPIの人日表示を小数第1位に統一／ラベル変更／保守状況・受注状況の工数表示チェックボックス削除（rev_20260821_a5c72f8）
- 「実績 / 総工数（人日）」（対応工数（保守））、「保守契約外（人日）」（対応工数（保守））、「実績 / 見積 工数（人日）」（受託工数）の3つのKPI値を、共通ヘルパー`fmt1()`で小数第1位まで表示するように統一（例：`9` → `9.0`）。ただし値が0の場合のみ従来通り整数の`0`と表示する（`0.0`にはしない）。
- 対応工数（保守）のKPIラベル「保守費設定済み取引先」を「保守契約済み取引先」に変更。
- 保守状況・受注状況タブの期セレクタ横にあった「工数表示」チェックボックス（`#hours-toggle-cb`）を削除。保守状況の対応工数関連表示（KPI「対応工数計」・月次推移グラフの対応工数の面・凡例・脚注）は、従来のチェックボックス既定値（ON）のまま常時表示する仕様に変更。受注状況側は元々このチェックボックスに連動する表示がなかったため、UIから消えるのみで表示内容に変更はない。
- 併せて未使用になった`showHours`変数・`toggleHours()`関数、`termBarHtml()`の`showHoursToggle`引数を削除しコードを整理。
- Playwrightで両タブのチェックボックスが表示されなくなったこと、保守状況の対応工数表示（KPI・グラフ・凡例）が従来通り表示されること、3つのKPIが小数第1位で表示されること、値0のケースで「0」（「0.0」ではない）と表示されることを確認済み。全4サブタブ・稼働状況3ビューの回帰確認も実施。

## 保守状況の対応工数表示を削除／全体キャパKPI統合／各KPI行の並び替え（rev_20260821_b91e5d3）
- 保守状況のダッシュボード（KPI「対応工数計（人日）」）と月次推移グラフ（対応工数の面・右軸・凡例・脚注）から対応工数表示を完全に削除。件数（発生・完了）のみのシンプルな表示に戻した。合わせて未使用となった対応工数の月次集計処理も削除。
- 全体キャパのKPI「基本稼働（人日・経過Nヶ月）」「実績合計（保守＋受託・人日）」の2枚を、「実績／ベース稼働（人日）」1枚（`実績値 / ベース稼働値`、小数第1位表示）に統合。
- 対応工数（保守）のKPI行の並びを、「保守契約済み取引先」を先頭（「残予算（円）」の左）に移動。
- 受託工数のKPI行の並びを、「対象取引先数」を先頭（「受注額合計」の左）に移動。
- Playwrightで保守状況のチャートに右軸・対応工数の面が出なくなったこと、全体キャパの統合KPIの値、対応工数（保守）・受託工数それぞれのKPI並び順、全4サブタブ・稼働状況3ビューでpageerrorが出ないことを確認済み。
